package com.virtualqueue.pageobjects.tablet;

import com.virtualqueue.pageobjects.BasePage;
import org.openqa.selenium.WebDriver;

public abstract class BaseTabletPage extends BasePage {
    public BaseTabletPage(WebDriver driver) {
        super(driver);
    }
}
